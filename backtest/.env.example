# KRX 계정 정보
#
# 사용법:
#   1. 이 파일을 .env 로 복사     cp .env.example .env
#   2. 아래 값을 본인 계정으로 수정
#   3. 저장 후  python backtest.py --check-krx  로 확인
#
# .env 는 .gitignore 에 등록되어 있어 GitHub 에 올라가지 않습니다.
# 이 .env.example 파일에는 절대 실제 계정을 적지 마십시오.

KRX_ID=여기에_아이디
KRX_PW=여기에_비밀번호
